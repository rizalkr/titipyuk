# Production Environment Variables (example)
# Ganti placeholder dengan nilai asli. Jangan commit file yang berisi secret asli.

# Supabase (public)
NEXT_PUBLIC_SUPABASE_URL=https://<PROJECT_REF>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<YOUR_ANON_KEY>

# (Opsional) Server-side duplicate jika kode server baca non-prefixed
SUPABASE_URL=https://<PROJECT_REF>.supabase.co
SUPABASE_ANON_KEY=<YOUR_ANON_KEY>
# Service role (JANGAN pernah diberi prefix NEXT_PUBLIC). Hanya di env server (Vercel Project Settings), bukan di .env lokal yang ter-commit.
SUPABASE_SERVICE_ROLE_KEY=<YOUR_SERVICE_ROLE_KEY>

# Chatbot Lunos
LUNOS_BASE_URL=https://api.lunos.tech/v1
LUNOS_API_KEY=<YOUR_LUNOS_API_KEY>

# Tambahan (contoh jika nanti butuh)
# RATE_LIMIT_WINDOW=60000
# RATE_LIMIT_MAX=100

# Pastikan file ini tidak berisi key asli saat di-commit.
